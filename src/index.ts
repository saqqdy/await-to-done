/**
 * Async await wrapper for easy error handling
 *
 * @param instance - axios or axios instance
 * @param options - serializer options
 * @return - axios instance with serializer feature
 */
export default function awaitToDone() {
	//
}
